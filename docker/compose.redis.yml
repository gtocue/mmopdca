# docker/compose.redis.yml  ── Redis のカスタム設定を追加したいとき用
services:
  redis:
    # 下記のように上書きしたい行をアンコメントして利用
    # command: >
    #   redis-server --requirepass ${REDIS_PASSWORD}
    #                --maxmemory 512mb --maxmemory-policy allkeys-lru
