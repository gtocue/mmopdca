# docker/compose.redis.yml
#
# ── Redis のみを個別 override したくなった時用の “空テンプレ”。
version: "3.9"

services:
  redis:
    # 例：ボリュームや maxmemory を環境ごとに変える等
    # command: >
    #   redis-server --requirepass ${REDIS_PASSWORD} --maxmemory 512mb
