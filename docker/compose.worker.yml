# docker/compose.worker.yml
#
# ── Worker のスケール値だけを宣言するレイヤ、E
#    ※ docker compose (Swarm でなぁE では deploy.replicas は無視されるので
#       実コンチE��数を変えたい場合�E `docker compose up --scale worker=3 …`
#       もしく�E Swarm/K8s に移行した時だけ有効になる、と忁E��てください、E

services:
  worker:
    deploy:
      replicas: 3
