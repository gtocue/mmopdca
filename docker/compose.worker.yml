version: "3.9"

services:
  beat:
    # Celery Beat のみを起動
    command:
      - celery
      - -A
      - core.celery_app:celery_app
      - beat
      - --loglevel=info
      # スケジュールファイル配置先を変える場合はここに --schedule=... を追加
