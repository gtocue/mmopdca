services:
  worker:
    # Celery Worker を直接起動するコマンドに変更
    command:
      - celery
      - -A
      - core.celery_app:celery_app
      - worker
      - --loglevel=info
    # …（以下は既存の deploy, entrypoint, environment 等そのまま）


