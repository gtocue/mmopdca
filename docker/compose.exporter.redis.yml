# docker/compose.exporter.redis.yml  ── Redis-exporter を個別に公開
services:
  redis-exporter:
    image: oliver006/redis_exporter:v1.60.0
    command:
      - "--redis.addr=redis://default:${REDIS_PASSWORD_ENC}@redis:6379"
    restart: unless-stopped
    ports:
      - "9121:9121"
