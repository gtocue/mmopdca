# docker/compose.exporter.redis.yml  笏笏 Redis-exporter 繧貞句挨縺ｫ蜈ｬ髢・
services:
  redis-exporter:
    image: oliver006/redis_exporter:v1.60.0
    command:
      - "--redis.addr=redis://default:${REDIS_PASSWORD_ENC}@redis:6379"
    restart: unless-stopped
    ports:
      - "9121:9121"

