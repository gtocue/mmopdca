# docker/compose.nginx.yml  ── エッジで HTTPS(443) のみを晒す最小構成
services:
  nginx:
    ports:
      - "443:443"
    # 追加の vhost / 静的サイトをマウントしたい場合は以下を有効化
    # volumes:
    #   - ./ops/extra-site.conf:/etc/nginx/conf.d/extra-site.conf:ro
