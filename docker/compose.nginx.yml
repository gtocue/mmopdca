# docker/compose.nginx.yml

services:
  nginx:
    # ── Edge 公開時だぁE443 を開ける
    ports:
      - "443:443"
    # 追加 vhost / static などは↓を参老E��
    # volumes:
    #   - ./ops/extra-site.conf:/etc/nginx/conf.d/extra-site.conf:ro
