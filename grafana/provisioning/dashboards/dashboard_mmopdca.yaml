apiVersion: 1
providers:
  - name: mmopdca-provider
    type: file
    folder: MMOPDCA
    allowUiUpdates: true
    overwrite: true      # ← ★ここを追加
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
