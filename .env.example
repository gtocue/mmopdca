###############################################################################
# .env.example ― “この 1 枚” を .env にコピーすれば compose が完走する
###############################################################################

# ---------- Database ----------
POSTGRES_PASSWORD = SuperSafePgPassw0rd!

# ---------- Redis ----------
REDIS_PASSWORD    = <ここに ops\redis_pwd.txt の Base64 を貼る>
REDIS_URL         = redis://:${REDIS_PASSWORD}@redis:6379/0

# ---------- Celery ----------
CELERY_BROKER_URL     = ${REDIS_URL}
CELERY_RESULT_BACKEND = ${REDIS_URL}

# ---------- そのほか（必要なら書き換え） ----------
LOG_LEVEL   = INFO
TZ          = UTC
