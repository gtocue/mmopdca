###############################################################################
# .env   E2025-05-06  *clean / exporter-safe*
###############################################################################

# ---------- Redis ----------
# ① RAW�E�E めE= を含む�E�… 既存サービス用
REDIS_PASSWORD=<RAW_PASSWORD>

# ② URL-ENCODED … Celery-exporter / URL 外向き用
REDIS_PASSWORD_ENC=<URL_ENCODED>

# ---------- PostgreSQL ----------
POSTGRES_DB=mmopdca
POSTGRES_USER=mmop_user
POSTGRES_PASSWORD=SuperSafePgPassw0rd!

# ---------- Paths ----------
DSL_ROOT=/mnt/data/dsl

# ---------- Celery ----------
# *exporter は CE_BROKER_URL を読むので下記�E **コメントアウチEor 削除***
# CELERY_BROKER_URL=redis://default:${REDIS_PASSWORD}@redis:6379/0
# CELERY_RESULT_BACKEND=${CELERY_BROKER_URL}

# ---------- Misc ----------
LOG_LEVEL=INFO
TZ=UTC
